//
//  Physics.cpp
//  Graphics-Final-Project
//
//  Created by Meraj Patel on 12/3/2013.
//  Copyright (c) 2013 Kirisanth Subramaniam. All rights reserved.
//

#include "Physics.h"


Physics::Physics()
{
    
};

//Physics::~Physics()
//{
//    
//};

void Physics::moveParticle(Particle ball)
{
//    ball.currentPositionX += (movementDirectionX * time) + (0.5 * (movementAccelerationX * (time * time)));
//    ball.movementDirectionX = movementDirectionX + (movementAccelerationX * time);
//    
//    ball.currentPositionY += (movementDirectionY * time) + (0.5 * (movementAccelerationY * (time * time)));
//    ball.movementDirectionY = movementDirectionY + (movementAccelerationY * time);
//    
//    ball.currentPositionZ += (movementDirectionZ * time) + (0.5 * (movementAccelerationZ * (time * time)));
//    ball.movementDirectionZ = movementDirectionZ + (movementAccelerationZ * time);
    
};

void Physics::collision(std::vector<Particle>& ball, std::vector<ObjectsModel>& vector);
{
    //iterations for all objects
    //iteration for objects (cube)
//    for (int i=0; i < test.size(); i++) {
//        test.at(i).velZ = 0
//    }
//    //iteration for walls
//    for (int i=0; i < test.size(); i++) {
//        test.at(i).velZ = 0
//    }
//    //iteration for
//    for (int i=0; i < test.size(); i++) {
//        test.at(i).velZ = 0
//    }
};
